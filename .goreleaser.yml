version: 2

before:
  hooks:
    - go mod download
    - go mod tidy

builds:
  - id: tack-darwin
    ldflags:
      - -s -w -X github.com/k1LoW/tack/version.Revision={{.ShortCommit}}
    flags:
      - -trimpath
    env:
      - CGO_ENABLED=0
    goos:
      - darwin
    goarch:
      - amd64
      - arm64
  - id: tack-linux
    ldflags:
      - -s -w -X github.com/k1LoW/tack/version.Revision={{.ShortCommit}}
    flags:
      - -trimpath
    env:
      - CGO_ENABLED=0
    goos:
      - linux
    goarch:
      - amd64
      - arm64
  - id: tack-windows
    ldflags:
      - -s -w -X github.com/k1LoW/tack/version.Revision={{.ShortCommit}}
    flags:
      - -trimpath
    env:
      - CGO_ENABLED=0
    goos:
      - windows
    goarch:
      - amd64
      - arm64

archives:
  - id: tack-archive
    name_template: "{{ .ProjectName }}_v{{ .Version }}_{{ .Os }}_{{ .Arch }}{{ if .Arm }}v{{ .Arm }}{{ end }}"
    format_overrides:
      - goos: darwin
        format: zip
      - goos: windows
        format: zip
    files:
      - LICENSE
      - README.md
      - CHANGELOG.md
      - CREDITS

checksum:
  name_template: checksums.txt

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"

nfpms:
  - id: tack-nfpms
    builds:
      - tack-linux
    maintainer: "Ken'ichiro Oyama <k1lowxb@gmail.com>"
    description: "Deploy static websites to Tailor Platform"
    license: MIT
    formats:
      - deb
      - rpm
      - apk
    bindir: /usr/bin

release:
  use_existing_draft: true
